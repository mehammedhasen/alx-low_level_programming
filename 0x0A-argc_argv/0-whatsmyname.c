#include "main.h"
/**
* main - Entry point of function
* @argc: first ag
* @argv: second arg
* Return: Always 0
*/
int main(int argc, char *argv[])
{
if (argc > 0)
printf("%s\n", argv[0]);
return (0);
}
