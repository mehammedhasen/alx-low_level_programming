#include "main.h"
/**
* _isupper - check that if upper or lower case
* @i: number to be check
* Return: 1 for upper else 0
*/

int _isupper(int i)
{
if (i >= 65 && i <= 90)
{return (1);
}
return (0);
}
